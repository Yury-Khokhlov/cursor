export function Hero() {
  return (
    <header className="pb-8 pt-4">
      <p className="max-w-xl font-mono text-[10px] font-medium uppercase leading-relaxed tracking-[0.16em] text-muted sm:text-[11px] sm:tracking-[0.18em]">
        An application in the form of a product thesis
      </p>
      <h1 className="mt-6 max-w-[20ch] text-[clamp(2rem,5.5vw,3.5rem)] font-semibold leading-[1.06] tracking-tight text-foreground sm:max-w-[22ch]">
        Cursor should fork the user experience, not the mission.
      </h1>
    </header>
  );
}
